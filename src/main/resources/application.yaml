spring:
  config:
    activate:
      on-profile: postgres
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/winelottery
    password: multipass123
    username: wineuser
  jpa:
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create
  thymeleaf:
    enabled: true
    prefix: classpath:/templates/
    suffix: .html

server.port: 8094